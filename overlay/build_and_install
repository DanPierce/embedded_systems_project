#!/bin/bash

echo "Compiling the overlay from .dts to .dtbo"

dtc -O dtb -o EBB-2PRU-GPIO-00A0.dtbo -b 0 -@ EBB-2PRU-GPIO.dts 


sudo rm /lib/firmware/EBB-2PRU-GPIO-00A0.dtbo

echo "Copying to /lib/firmware"

sudo cp EBB-2PRU-GPIO-00A0.dtbo /lib/firmware/